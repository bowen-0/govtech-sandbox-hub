# macOS
.DS_Store

# Claude Code local settings (not for sharing)
.claude/
# …except the wiki's portable skills, which ship with the corpus so any
# Claude Code / Agent SDK consumer of the wiki inherits the query procedure.
!wiki/.claude/
!wiki/.claude/**

# Editor
.vscode/
.idea/
*.swp
.obsidian/

# Node (for when the actual project repo is created here)
node_modules/
.next/
out/
dist/
.env
.env.local
.env.*.local

# Python (for ingest pipeline)
__pycache__/
*.pyc
.venv/
venv/
.python-version
