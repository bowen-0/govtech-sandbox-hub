{
  "name": "sandbox-hub-agent-worker",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker shim that fronts the LangGraph Python container.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types --env-interface Env worker/env.d.ts"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.95.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "workerd"]
  }
}
